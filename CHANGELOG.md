# 更新日志

本文件记录面向用户的发行变化。内部调查、个人路径、测试账号与密钥不进入公开更新日志。

## 1.0.1 — 2026-08-04

### 基础可用性

- 修正 AI 服务源首次保存、配置应用与重新读取链，避免已保存后仍提示未配置。
- 收紧聊天消息删除与回档后的索引传导，避免前端继续提交已经失效的位置。
- 补齐部件配置应用、生命周期取消、失败回滚与熔断边界，减少波动时的重复加载和状态假成功。
- 修正多窗口文件工作区与可信会话的绑定链，保留主窗口自动绑定和额外窗口显式绑定。

### 首次使用与配置

- 新用户默认提供“新的开始”角色卡，并增加语言选择与启动加载状态。
- AI 未配置时的入口改为直接引导到完整设置；子模式未绑定 API 时给出独立提示。
- 子模式默认采用严格后处理、不预填充、1,000,000 上下文和 30,000 最大输出；用户仍可修改。

### P1、YonBan 与 CLI

- 同步 P1 自驱动运行时、资源目录与诊断展示，补充启动波动、有限重试和宿主退出联动。
- 同步 YonBan IDE、扩展端与本体通信链，以及 CLI、文件、检查点和命令工具。
- 保留公共发布所需的 P1 资源、YonBan 和 CLI 依赖；运行时生成物与用户状态不进入仓库。

### 更新与数据保护

- 复用单一 Git 更新链：启动后检查 `origin/main`，只允许 fast-forward 更新；网络失败、分叉或已跟踪文件被修改时保持当前版本并提示。
- `data/`、`storage/`、用户配置、角色卡、记忆、日志、缓存和未跟踪插件不参与更新覆盖。
- EXE 从公共仓库干净工作树打包，保留离线运行时与 Git 元数据，使安装本版后可以继续使用同一安全更新链。

### 文档

- 更新多语言 README、内置 Wiki、P1 当前运行结构、YonBan、CLI、插件组合、API 配置与可靠性契约。

### 验证边界

- 发行前执行语法、清单、隐私/密钥、文件边界与产物哈希检查。
- 本轮不代替用户的真实后端、浏览器、模型、WebSocket 与长时间运行测试；运行验证结果以用户测试为准。
